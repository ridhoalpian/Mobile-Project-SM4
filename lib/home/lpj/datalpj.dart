import 'package:flutter/material.dart';

class dataLpj extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'Ini adalah halaman data lpj',
          style: TextStyle(fontSize: 20),
        ),
      ),
    );
  }
}