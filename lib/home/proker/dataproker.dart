import 'package:flutter/material.dart';

class dataProker extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text(
          'Ini adalah halaman data proker',
          style: TextStyle(fontSize: 20),
        ),
      ),
    );
  }
}